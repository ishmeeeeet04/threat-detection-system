services:
  - type: web
    name: threat-detection-system
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python startup.py && gunicorn "backend.app:create_app()" --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0